apiVersion: v1
kind: Service
metadata:  
  name: minautor-sapient
  annotations:
    app.access.url: < URL >
spec: 
  type: ClusterIP
  ports: 
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: minautor-sapient


    
