apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
  
resources:
    - service.yaml
    - deployment.yaml
    - namespace.yaml
    - pvc.yaml
    - ingress.yaml
generatorOptions:
 disableNameSuffixHash: true

