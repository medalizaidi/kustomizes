apiVersion: v1
kind: Service
metadata:  
  name: minotaur-frontend
  annotations:
    app.access.url: < URL >
spec: 
  type: < TYPE >
  ports: 
  - name: < PORT-NAME >
    port: < PORT >
    protocol: < PROTOCOL >
    targetPort: < TARGETPORT >
  selector:
    app: < SELECTOR-APP >


    
